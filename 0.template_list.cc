#include "leetcode.hpp"
#include <bits/stdc++.h>

using namespace std;

void solve() {}

int main() {
  string s;
  ListNode *list;
  getline(cin, s);
  list = stringToListNode(s);
  cout << list;
}