#include "leetcode.hpp"
#include <bits/stdc++.h>

using namespace std;

void solve() {}

int main() {
  string s;
  TreeNode *root;
  getline(cin, s);
  root = stringToTreeNode(s);
  cout << root;
}